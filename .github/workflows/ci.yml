name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  quality:
    name: Code quality
    runs-on: ubuntu-24.04

    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install project
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Install security tools
        run: |
          pip install pip-audit bandit[toml]

      - name: Audit dependencies
        # --ignore-vuln is not needed here; --skip-editable skips local packages
        # that are not published to PyPI (like this project in its first PR)
        run: pip-audit --skip-editable

      - name: Lint (ruff)
        run: |
          if find . -name "*.py" -not -path "./.venv/*" | grep -q .; then
            ruff check .
          else
            echo "No Python source files yet — skipping ruff lint"
          fi

      - name: Format check
        run: |
          if find . -name "*.py" -not -path "./.venv/*" | grep -q .; then
            ruff format --check .
          else
            echo "No Python source files yet — skipping ruff format check"
          fi

      - name: Type check
        run: |
          if [ -d "AMIP" ]; then
            mypy AMIP/
          else
            echo "AMIP/ directory does not exist — skipping mypy"
          fi

  tests:
    name: Tests (Python ${{ matrix.python-version }})
    runs-on: ubuntu-24.04
    needs: quality

    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install project
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Install security tools
        run: |
          pip install pip-audit bandit[toml]

      - name: Audit dependencies
        run: pip-audit --skip-editable

      - name: Run tests
        run: |
          if find . -name "*.py" -not -path "./.venv/*" | grep -q .; then
            pytest -v --cov=. --cov-report=term-missing
          else
            echo "No Python source files yet — skipping pytest"
          fi

  security:
    name: Security scan
    runs-on: ubuntu-24.04
    needs: quality

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install security tools
        run: |
          pip install pip-audit bandit[toml]

      - name: Audit dependencies
        run: pip-audit --skip-editable

      - name: Static security scan
        run: |
          if find . -name "*.py" -not -path "./.venv/*" | grep -q .; then
            bandit -r . -c pyproject.toml
          else
            echo "No Python source files yet — skipping bandit scan"
          fi

  dependency-review:
    name: Dependency review
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-24.04
    needs: quality

    steps:
      - uses: actions/checkout@v4

      - name: Dependency Review
        uses: actions/dependency-review-action@v4
